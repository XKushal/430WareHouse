package gui;
import warehouse.*;
import java.util.Iterator;

public class ListOutstandingState extends WarehouseState{

    private static ListOutstandingState instance;

    private ListOutstandingState(){

    }

    public static ListOutstandingState instance() {
        if ( instance == null )
            return instance = new ListOutstandingState();
        return instance;
    }

    ListOutstandingPanel panel = new ListOutstandingPanel();

    public void run()
    {
        panel.setup();
        changePanel(panel);
    }

    public void confirmPressed(){
        panel.resultTextBox.append("Not yet implemented");
//        Iterator clients = Warehouse.instance().getClientsWithOustandingBalance(0.0);
//        panel.resultTextBox.append("Clients with outstanding balances: \n");
//        while(clients.hasNext()){
//            Client c = (Client) clients.next();
//            panel.resultTextBox.append("Client " + c.getId() + ", " + c.getName() + ", has an outstanding balance of $" + c.getBalance() + "\n");
//        }
    }
}
